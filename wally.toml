[package]
name = "arenn22/connection-hub"
description = "A centralized way to connect to the same event multiple times"
license = "MIT"
version = "1.0.0"
registry = "https://github.com/UpliftGames/wally-index"
repository = "https://github.com/arenn22/Connection-Hub"
realm = "shared"
exclude = ["rokit.toml"]

[dependencies]
